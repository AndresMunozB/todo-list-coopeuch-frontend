import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  logoutButton: {
    marginLeft: 'auto'
  },
  titleLabel: {
    marginLeft: 15
  }
}));

export default useStyles;
